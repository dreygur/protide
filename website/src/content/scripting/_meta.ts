import type { MetaRecord } from "nextra";

export default {
  overview: "Overview",
  expect: "expect() reference",
  chaining: "Request chaining",
  runner: "Collection runner",
} satisfies MetaRecord;
